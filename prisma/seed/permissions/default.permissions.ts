export const GLOBAL_PERMISSIONS = ['UPDATE_PROFILE', 'VIEW_PROFILE'];

// In future create some default admin and their permissions

export const CONSUMER_DEFAULT_PERMISSIONS = [...GLOBAL_PERMISSIONS];

export const PROVIDER_DEFAULT_PERMISSIONS = [...GLOBAL_PERMISSIONS];
